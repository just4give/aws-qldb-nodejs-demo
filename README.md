# Build & deploy code for lambda

```
cd lambda
npm install
cd ..
sh deploy-test.sh
```

# Run Ionic app

Update src/environment/environment.ts file with API Gateway URL
```
cd ionic-app
npm install
ionic serve
```
